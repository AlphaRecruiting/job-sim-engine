[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @job-sim/shared build",
  "pnpm --filter @job-sim/web build"
]

[start]
cmd = "pnpm --filter @job-sim/web start"
